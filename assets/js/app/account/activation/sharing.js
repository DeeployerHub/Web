(function(angular) {
    'use strict';

    angular.module("deeployer", [])
    .controller("SharingController", [
        '$scope', '$http', 
        function ($scope, $http) {
        }
    ]);
})(window.angular);
